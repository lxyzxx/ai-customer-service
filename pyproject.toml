[project]
name = "ai-customer-service"
version = "0.1.0"
description = "Minimal agentic customer service demo with direct corpus retrieval, context checks, chat, and source tracing."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
