[project]
name = "ai-qa-bot"
version = "0.1.0"
description = "Minimal internal question answering bot with direct corpus retrieval, context checks, chat, and source tracing."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
